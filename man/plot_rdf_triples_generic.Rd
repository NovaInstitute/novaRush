% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rdf_triples_generic_p.R,
%   R/plot_rdf_tripples_generic.R
\name{plot_rdf_triples_generic}
\alias{plot_rdf_triples_generic}
\title{plot_rdf_triples_generic}
\usage{
plot_rdf_triples_generic(
  triples_df,
  show_edge_labels = TRUE,
  stabilize = TRUE,
  shorten_ids = TRUE,
  object_types = NULL,
  predicates = NULL,
  subjects = NULL,
  objects = NULL,
  gravitationalConstant = -2000,
  centralGravity = 0.3,
  springLength = 95,
  springConstant = 0,
  damping = 0.09,
  avoidOverlap = 0.1
)

plot_rdf_triples_generic(
  triples_df,
  show_edge_labels = TRUE,
  stabilize = TRUE,
  shorten_ids = TRUE,
  object_types = NULL,
  predicates = NULL,
  subjects = NULL,
  objects = NULL,
  gravitationalConstant = -2000,
  centralGravity = 0.3,
  springLength = 95,
  springConstant = 0,
  damping = 0.09,
  avoidOverlap = 0.1
)
}
\arguments{
\item{triples_df}{A tibble with columns \code{subject}, \code{predicate}, \code{object}, and optionally \code{type}.}

\item{show_edge_labels}{Logical, whether to display edge labels (default: TRUE).}

\item{stabilize}{Logical, whether to enable layout stabilization (default: TRUE).}

\item{shorten_ids}{Logical, whether to shorten node and edge labels (default: TRUE).}

\item{object_types}{Character vector, filter objects by \code{type} column (default: NULL, no filtering).}

\item{predicates}{Character vector, filter by specific predicates (default: NULL, no filtering).}

\item{subjects}{Character vector, filter by specific subjects (default: NULL, no filtering).}

\item{objects}{Character vector, filter by specific objects (default: NULL, no filtering).}

\item{gravitationalConstant}{Numeric, negative force for node repulsion (default: -2000).}

\item{centralGravity}{Numeric, attraction to graph center (default: 0.3).}

\item{springLength}{Numeric, ideal edge length (default: 95).}

\item{springConstant}{Numeric, edge stiffness (default: 0.04).}

\item{damping}{Numeric, motion damping (default: 0.09).}

\item{avoidOverlap}{Numeric, node overlap prevention (default: 0.1).}
}
\value{
A visNetwork object representing the interactive graph.

A visNetwork object representing the interactive graph.
}
\description{
Create an interactive network diagram from a tibble of RDF triples, treating all objects as nodes, with stabilized layout, optional edge labels, optional ID shortening, complex query filtering, and customizable physics parameters.

Create an interactive network diagram from a tibble of RDF triples, treating all objects as nodes, with stabilized layout, optional edge labels, optional ID shortening, complex query filtering, and customizable physics parameters.
}
\examples{
triples <- tibble::tibble(
  subject = c("id1", "id1", "id1", "id1", "id1", "id2"),
  predicate = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width", "Species", "Sepal.Length"),
  object = c("5.1", "3.5", "1.4", "0.2", "setosa", "4.9"),
  type = c("double", "double", "double", "double", "tag", "double")
)
p <- plot_rdf_triples_generic(
  triples,
  show_edge_labels = FALSE,
  shorten_ids = TRUE,
  object_types = c("double"),
  gravitationalConstant = -3000,
  springLength = 150
)
htmlwidgets::saveWidget(p, file = "rdf_graph.html", selfcontained = TRUE)
utils::browseURL("rdf_graph.html")
triples <- tibble::tibble(
  subject = c("id1", "id1", "id1", "id1", "id1", "id2"),
  predicate = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width", "Species", "Sepal.Length"),
  object = c("5.1", "3.5", "1.4", "0.2", "setosa", "4.9"),
  type = c("double", "double", "double", "double", "tag", "double")
)
p <- plot_rdf_triples_generic(
  triples,
  show_edge_labels = FALSE,
  shorten_ids = TRUE,
  object_types = c("double"),
  gravitationalConstant = -3000,
  springLength = 150
)
htmlwidgets::saveWidget(p, file = "rdf_graph.html", selfcontained = TRUE)
utils::browseURL("rdf_graph.html")
}
